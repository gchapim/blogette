<div class="container">
  <div class="head">
    <h1>Create User</h1>
  </div>

  <%= form_with model: @user, url: admin_users_path do |f| %>
    <div class="form-group">
      <label for="user-email">Email</label>
      <%= f.email_field(:email, class: 'form-control', id: 'user-email', placeholder: 'thanos@blogette.com') %>
      <label for="user-password">Password</label>
      <%= f.password_field(:password, class: 'form-control', id: 'user-password') %>
    </div>

    <div class="form-group">
      <%= f.submit 'Create user', class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>
